# Purpose #

This directory will hold those third party dlls which cannot be obtained via Nuget.

With Nuget package restore, there is no need to have dlls checked in to the source control. However, we will still have to store assemblies for libraries which are not obtainable via Nuget. 