@model ListSegmentsViewModel

@{
    ViewBag.Title = "title";
    Layout = "/Views/Shared/_LayoutAdmin.cshtml";

}

<div id="MailchimpListContainer" class="container">


    <h2>title</h2>
    <div>
        @{
            <h2>No List</h2>
            foreach (var affiliate in Model.NoList)
            {
                <div>@affiliate.ttsDomain</div>

            }
            <h2>No Segment Found</h2>
            foreach (var affiliate in Model.HaveNoSegment)
            {
                <div>@affiliate.ttsDomain</div>

            }
            <h2>No List</h2>
            foreach (var affiliate in Model.NoList)
            {
                <div>@affiliate.ttsDomain</div>
                foreach (var modelAffAndListSegment in Model.AffAndListSegments)
                {
                    var a = 1;
                    //if (modelAffAndListSegment.Keys)
                }
            }

        }

    </div>
</div>