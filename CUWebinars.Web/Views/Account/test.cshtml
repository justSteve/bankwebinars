@{


}
@* ReSharper disable JSLint *@
<script language="javascript" type="text/javascript">

    function getZip() {
        $("#getZip").show();
        $(".loginContainer").hide();
    }
    var ds = "";
    $(document).ready(function () {
        $.support.cors = true;

        //$.getJSON("http://localhost:2524/account/GetSageIDsAreNull?callback=?",
        //    function (data) {
        //        var mydata = $.parseJSON(data);
        //        alert(mydata);

        //        //for(i in data[1]) {
        //        //    alert(i);
        //        //}
        //});
        $.ajax({
            //contentType: 'application/jsonp',
            crossDomain: true,
            url: "http://localhost:2524/account/GetSageIDsAreNull",
            type: 'get',
            dataType: 'json',
            cache: false,
            async: false,
            complete: function (xhr, textStatus) {
                var message = "\r\ntextStatus: " + textStatus +
                    "\r\nContentType: " + xhr.getResponseHeader("Content-Type") +
                    "\r\nStatus: " + xhr.status +
                    "\r\nResponseText:\r\n" + xhr.responseText;

                alert("complete " + message);
            },
        }).success(function (response) {
            alert("Success: " + response.data);
            //}).error(function (response) {
            //    app.showNotification("Error", response.statusText);
            //

        }).error(function (response) {
            alert("error: " + response.statusText);
            //}).error(function (response) {
            //    app.showNotification("Error", response.statusText);
            //
        });
        //    $("#txt_speciality").tokenInput("add", {
        //        name: value.name,
        //        id: value.id
        //    });
        //});
        //$.each(data, function (dt) {

        //    $("#txt_speciality").tokenInput("add", mydata.id);
        //});

    });

</script>
@* ReSharper restore JSLint *@
<style>
    .fieldContainer {
        /*width: 380px;*/
        line-height: 25px;
    }

    .span2 {
        text-align: right;
    }
</style>
<h4 id="txt_speciality">test</h4>
<div></div>
