@model  MyWebinarsDTO
@using CUWebinars.Web.Core;
@using CUWebinars.Business.Models


@{
    ViewBag.Title = "My Webinars";
    Layout = "/Views/Shared/_LayoutPublic.cshtml";


    WebUser currentUser = Model.WebUser;
    var EditResult = "<span>&nbsp;</span>";
    var userSubscription = new DiscountDetailsModel() { Discount = Model.Subscription, UserId = currentUser.idUser };
    var userPackage = new DiscountDetailsModel() { Discount = Model.Package, UserId = currentUser.idUser };
    if (TempData["EditResult"] != null && (string)TempData["EditResult"] != "")
    {
        EditResult = TempData["EditResult"].ToString();
    }

    if (@Html.ValidationMessage("GeneralError") != null)
    {
        EditResult = "ERROR: " + @Html.ValidationMessage("GeneralError").ToHtmlString();
    }
}

@*<link href="/Content/css/home_index.css" rel="stylesheet" type="text/css" />*@
@*<script type="text/javascript">
        function resendConfirmation(form) {
            form.parent.submit();
        }
    </script>*@

<div class="container">
    <h2 class="short_headline">My Webinars</h2>
    @{
    //var thisDiscount = PageData["thisUser"].SubscriptionDiscount;

    }
    @*<a href="/account/manage/@currentUser.idUser?returnUrl=/Account/MyWebinars/@currentUser.idUser">
            <span style="color: black;" class="pull-right btn btn-success">
                Edit My Account Profile
            </span>
        </a>*@
    @{var pModel = new ResetPasswordModel
      {
          Email = currentUser.email,
          EmailSent = false
      };}
    @*<span style="margin-left: 25px;"><input type="button" class="btn-small btn btn-link" onclick="javascript: stateManager.passResetView(); return false;" value="Forgot Password?" /></span>
        Html.Partial("~/Views/Admin/_ResetPasswordPartial.cshtml", pModel)*@

    <div class="accordion" id="mywebinars">
        @*@if (userSubscription != null)
        {
            <div class="accordion-group">
                <div class="accordion-heading">
                    <a class="accordion-toggle" data-toggle="collapse" data-parent="#mywebinars" href="#subscription">
                        Compliance Perspectives Subscription
                    </a>

                    <div id="subscription" class="accordion-body collapse out">
                        <div class="accordion-inner">
                            @{Html.RenderPartial("Partials/Subscription", userSubscription);}
                        </div>
                    </div>
                </div>
            </div>
        }*@
        @*
        //still needs backing objects.        
        @if (userPackage != null)
            {
                <div class="accordion-group">
                    <div class="accordion-heading">
                        <a class="accordion-toggle" data-toggle="collapse" data-parent="#mywebinars" href="#package">
                            Package Subscription
                        </a>
                        <div id="package" class="accordion-body collapse out">
                            <div class="accordion-inner">
                                @{Html.RenderPartial("Partials/Package", userPackage);}
                            </div>
                        </div>
                    </div>
                </div>
            }*@
        @if (Model.Scheduled.Count > 0)
        {
            <div class="accordion-group">
                <div class="accordion-heading">
                    <a class="accordion-toggle" data-toggle="collapse" data-parent="#mywebinars" href="#upcoming1">
                        <h3>Upcoming Events</h3>
                    </a>
                    <div id="upcoming1" class="accordion-body collapse in">
                        <div class="accordion-inner">
                            @{ Html.RenderPartial("Partials/upcoming"); }
                        </div>
                    </div>
                </div>
            </div>
        }
        @if (Model.Recorded.Count > 0)
        {
            <div class="accordion-group">
                <div class="accordion-heading">
                    <a class="accordion-toggle" data-toggle="collapse" data-parent="#mywebinars" href="#recorded1">
                        <h3>Recorded Events</h3>
                    </a>
                    <div id="recorded1" class="accordion-body collapse in">
                        <div class="accordion-inner">
                            @{ Html.RenderPartial("Partials/Recorded"); }

                        </div>
                    </div>
                </div>
            </div>
        }
        @*<div class="accordion-group">
            <div class="accordion-heading">
                <a class="accordion-toggle" data-toggle="collapse" data-parent="#mywebinars" href="#editBilling">
                    Manage My Account
                </a>
                <div id="editBilling" class="accordion-body collapse out">
                    <div class="accordion-inner">
                        @{Html.RenderPartial("Partials/_EditBillingAddress");}
                    </div>
                </div>
            </div>
        </div>*@
        @*<div class="row-fluid">
                @{Html.RenderPartial("Partials/Messages");}
            </div>*@
        <br />
    </div>
    <div class="clear" />
    @*@{Html.RenderPartial("Partials/Archived");}*@
    @*<div class="modalOverlay" id="modalOverlay2">
            <p align="center">
                Simply list the required names in this box - one name per line. Type the names directly
                or prepare the list elsewhere and paste it here. When you click the 'Generate' button
                return a list of links that you can distribute to each attendee.</p>
            <form action="/Admin/registrations/CertificateOfCompletionList" id="frmCOCList" method="post">
            <input type="hidden" name="id" id="txtWebinarIDList" />
            <textarea cols="30" rows="8" name="displayNames">One name per line please.</textarea><p
                align="center">
                <input type="submit" value="Generate Certificates" /></p>
            </form>
        </div>
        <div class="modalOverlay" id="modalOverlay">
            <p align="center">
                <form action="/Admin/registrations/CertificateOfCompletion" id="frmCOC" method="post">
                <input type="hidden" name="id" id="txtWebinarID" />
                Name to Be Displayed:
                <input type="text" name="displayName" value="@PageData["thisUser"].FirstName  @PageData["thisUser"].LastName "  /><br />
                <br />
                <input type="submit" value="Generate Certificate" />
                </form>
            </p>
            <p align="center">
                <br />
                <br />
                Need to produce certificates for other attendees? <a href="javascript:ListOfNames();">
                    Click here.</a></p>
        </div>*@
</div>
