@model DiscountDetailsModel
@using System.Linq
@using CUWebinars.Web.Core
@using CUWebinars.Web.Helpers

@functions{


}

@{
    //this page displayed when WebUser.idSubscriptionDiscount maps to a CP subscription.
    //SQL would be:
    //UPDATE dbo.WebUser SET idSubscriptionDiscount = 353 WHERE email = 'you'
    
    //WebUser currentUser = Model.WebUser
    GlobalConfig globalConfig = GlobalConfig.GlobalConfigSingleton;

    //USTimeZone timeZ = currentUser.timeZone;
    //string email = Model.WebUser.email;
    //NumberFormatInfo nfi = new CultureInfo("en-US", false).NumberFormat;

    // nfi.CurrencyDecimalDigits = 0;
}
<style type="text/css">
    .audio {
        font-size: smaller;
        line-height: 98%;
        padding: 10px;
    }
</style>
<script language="javascript" type="text/javascript">


</script>

<div class="container">
    <!--begin main content-->
    <div class="row-fluid">
        <!--begin row-fluid-->
        <div class="span10">
            <h3>
                Subscription Details
            </h3>
            @using (Html.BeginForm("UpdateSubscriptionDetails", "Account", null, FormMethod.Post, new { id = "userDetailsForm", @class = "form-horizontal" }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary("Please address the following problems:", new { id = "userDetailsValSummary" })

                @Html.HiddenFor(m => m.UserId)
                //Editor does not render any controls.
                @Html.EditorFor(m => m.Discount)

                <button id="saveChangesButton" type="button" class="btn btn-primary offset2">Save changes</button>
                <div id="updateShippingMsgLabelWrap" style="display:inline-block"></div>
            }
        </div>
    </div>
</div>
