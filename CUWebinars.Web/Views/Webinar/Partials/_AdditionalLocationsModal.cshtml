@model AdditionalLocationOfferViewModel
@{
    var showPrice = string.Format("{0:C0}", @Model.Price);
}


<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">Additional Locations</h3>
    @*<div class="AddLocCaption">
            <blockquote style="margin-bottom: 0;">Want your branches to participate? Facilities within your organization will be able to participate without the travel costs of coming to one location.</blockquote>
        </div>*@
    <div class="AddLocCaption">
        <blockquote style="margin-bottom: 0;">Additional Locations can now be entered on the Order Confirmation screen.</blockquote>
    </div>
</div>
<div class="modal-body">

    @*<p><button id="AddInputsButton" class="btn btn-mini btn-success pull-right">Add Additional Location(s) - @showPrice</button></p>*@
    @{
        <div id="AdditionalLocationEmailWrapper">
            @{
                if (Model.OrderExists)
                {
                    for (int i = 0; i < Model.Emails.Count(); i++)
                    {
                        <span id="LocationSpan-@i">
                            <input id="AdditionalLocationEmail_@i" name="AddAdditionalLocationViewModel.Emails[@i]" type="text" value="@Model.Emails[i]" />
                            &nbsp;<i class="icon-trash icon-white" style="cursor: pointer" id="@i-AdditionLocationEmail-delete"></i>
                        </span>
                        <br id="@i-break" />
                    }
                }
            }
        </div>
        <div id="cCLocationsCaption">
            @using (Html.BeginForm("ShareNotifications", "Account", FormMethod.Post, new { id = "AdjustCcLocsForm", @class = "form-horizontal" }))
            {
                <input type="hidden" value="@Model.OrderExists" name="idOrder" />
                <div id="cCLocationsList"> </div>
                <div id="collectCcLocations"> </div>


            }


        </div>
        <div class="modal-footer">
            <button id="closeButton" class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
        </div>
    }
</div>

