@model AdditionalLocationOfferViewModel
<script type="text/javascript">
    var ADDLOC = {};
    ADDLOC.price = @Model.Price;

</script>
@{
    if ((Model.Webinar.Status == WebinarStatus.Scheduled
        || Model.Webinar.Status == WebinarStatus.Active
        || Model.Webinar.Status == WebinarStatus.InProgress
        ) && Model.Webinar.SeriesInfo != "DES")
    {
        <div id="AdditionalLocationContainer">
            <h5>Additional Locations</h5>

            @{ Html.Partial("Partials/_AdditionalLocationsModal"); }
            @if (Model.Webinar.Title.Contains("Compliance Perspectives"))
            {
                <div style="display: none" id="trial">
                    <p>Additional locations are not included in the trial subscription.</p>
                </div>
                <div id="first3" style="padding-left: 50px;">
                    Subscribers to the <b>Compliance Perspectives Series</b> receive the first three locations at no additional charge ($50 per connection after those first three):
                    <br />
                    <div style="padding-left: 50px;">
                        <input type="text" name="Email_0" id="Email_0" value="Email for Additional Location" class="{email: true}" /><br />
                        <input type="text" name="Email_1" id="Email_1" value="2nd Location" disabled="disabled" class="{email: true}" /><br />
                        <input type="text" name="Email_2" id="Email_2" value="3rd Location" disabled="disabled" class="{email: true}" /><br />
                    </div>
                </div>
                <div style="display: none" id="addLoc4Cost">
                    <button value="Add?" name="showAddLocModal" id="showAddLocModal">Add?</button>
                </div>
            }
            else
            {
                <div class="AddLocCaption">
                    <blockquote style="margin-bottom: 0;">Want your branches to participate? Facilities within your organization will be able to participate without the travel costs of coming to one location.</blockquote>
                </div>

                    <div style="padding-left: 50px;">
                        <span>
                            <b>Each additional location adds @Model.Price.ToString("c").Replace(".00", "") to your registration.</b>
                            <br /><a href="#AdditionalLocationsModal" role="button" class="btn" data-toggle="modal" id="AddLocationsButton">Add?</a>
                        </span>
                        <div id="collectAdditionalLocations"></div> @*This is where the inputs for each Additional Location will be dynamically created by the Modal dialog.*@
                    </div>

            }
            @if (Model.OrderExists)
            {
                Html.HiddenFor(m => m.WebUser.idUser);
            }
            else
            {
                @Html.Hidden("WebUser.idUser", 0, new { id = "WebUser_idUser" })
            }
            @Html.HiddenFor(m => m.Webinar.idWebinar)
        </div>
                }
    if (Model.Webinar.SeriesInfo == "DES")
    {
        <div><h4 style="font-style: normal;">Billing Details:</h4><p style="font-style: normal;">The term will commence on the START DATE of this agreement and continue for 6 or 12-months. This subscription will be renewable at the end of the current term for a successive 6 or 12-month term unless either party gives written notice of its intention not to renew 15 days before the expiration of the current term.We will notify you of any changes in the terms of the subscription at least 30 days prior to the notice period for renewal or termination.</p></div>
    }
}

@Scripts.Render("~/bundles/AdditionalLocation")
<!-- Modal -->
<div id="additionalLocationsModalDialog" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-body">
        @* modal content goes here *@
    </div><!-- /.modal-content -->
</div>
