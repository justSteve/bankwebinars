@model CUWebinars.Web.Models.ShowDiscountsModel
@{

    using (Html.BeginForm("GetUserDiscount", "Account", "Get"))
    {
        @Html.AntiForgeryToken()

        <div id="ShowDiscountContainer" class="well-large">
            <h3>Available Discount Information</h3>
        </div>
        <div class="form-inline">
            <div class="btn-group" style="width: 160px">&nbsp;</div>
            <div class="btn-group">
                <input type="submit" value="Submit" class="btn btn-default" />
            </div>
        </div>
    }
}