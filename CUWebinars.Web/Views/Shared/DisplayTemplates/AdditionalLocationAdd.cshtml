@using System.Linq
@model AdditionalLocationAddViewModel
    


    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title">Modal title</h4>
        <div>blabla</div>
    </div>
    <div class="modal-body">
        <div>
            blabla
        </div>
        <div>
            blabla
        </div>
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Save changes</button>
    </div>
    @*<b>Each additional location adds @Model.Price.ToString("c").Replace(".00", "") to your registration.</b>*@
    @*<b>Each additional location adds $150 to your registration.</b>
        <div id="AdditionalLocationContainer">

            @{
                var emails = Model.AdditionalLocations.Select(al => al.Email).ToArray();

                <ol>
                    @for (int i = 0; i < emails.Length; i++)
                    {
                        <li>@emails[i]</li>
                    }
                </ol>
            }

        </div>*@


<!-- Button to trigger modal -->






