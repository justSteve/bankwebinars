@using CUWebinars.Web.Core
@model Webinar

<div id="showHandouts-@Model.idWebinar ">
    @{
        foreach (var file in Model.WebinarFiles)
        {
            <a class="btn btn-warning" href='@GlobalConfig.GlobalConfigSingleton.HandoutRepository@file.fileLocation' target='_blank'>@file.fileDesc</a>
            <br />
        }
    }
</div>